<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  
  <title><![CDATA[koorchik's blog]]></title>
  <subtitle><![CDATA[Programming and IT-business]]></subtitle>
  <link href="/atom.xml" rel="self"/>
  <link href="http://blog.koorchik.com//"/>
  <updated>2016-09-11T08:11:31.000Z</updated>
  <id>http://blog.koorchik.com//</id>
  
  <author>
    <name><![CDATA[Viktor Turskyi]]></name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title><![CDATA[The Pain and the Joy of creating isomorphic apps in ReactJS]]></title>
    <link href="http://blog.koorchik.com/isomorphic-react/"/>
    <id>http://blog.koorchik.com/isomorphic-react/</id>
    <published>2015-12-08T10:43:36.000Z</published>
    <updated>2016-09-11T08:11:31.000Z</updated>
    <content type="html"><![CDATA[<p>This post is not a tutorial. There are enough of them on the Internet. It is always more interesting to look at real production app. A lot of tutorials and boilerplates show how to write isomorphic ReactJS applications, but they do not cover a lot of real-life problems we’ve faced in production. So we decided to share our experience at github and develop the app in a public repo. Here is <a href="http://itsquiz.com/en/activations" target="_blank" rel="external">the running app</a> and here is <a href="https://github.com/webbylab/itsquiz-wall" target="_blank" rel="external">its code</a>.</p>
<p><strong>IMPORTANT</strong>: This is real-world application, so it is always evolving. Our goal is to make working product within a limited amount of time. Sometimes, we have no time for finding the best possible solution, but use just suitable option for our case.</p>
<h2 id="Preface">Preface</h2><p>Let’s start with the term “Isomorphic application”. An isomorphic JavaScript application is just a JavaScript application that can run both client-side and server-side (in most cases it is a single-page application that can be run on server). I do not like the word “isomorphic”, I believe that programmers should struggle complexity (not only in code) and the word “isomorphic” complicates understanding, therefore it increases complexity :). There is another name for isomorphic JavaScript - “Universal JavaScript”, but in my opinion the word “Universal” is too general. So, in this post, I will use the word “isomorphic” ( even if do not like it :) ).</p>
<p>How do people see an isomorphic app? You can find diagramms in the internet like this one:</p>
<p><img src="isomorphic_separated.png" alt=""></p>
<p>But ideally it should look a little bit different:</p>
<p><img src="isomorphic_joined.png" alt=""></p>
<p>I mean, not less than 90% of your code should be reused. In large apps it can be even more than 95%.</p>
<p>If you want to develop isomorphic app from scratch, then start with reading this tutorial <a href="https://medium.com/@bananaoomarang/handcrafting-an-isomorphic-redux-application-with-love-40ada4468af4" target="_blank" rel="external">“Handcrafting an Isomorphic Redux Application (With Love)”</a>. It is really great!</p>
<h2 id="About_the_app">About the app</h2><p>In <a href="http://webbylab.com" target="_blank" rel="external">WebbyLab</a> we use React from the moment it was open sourced by Facebook almost in every our project. We’ve created keynote clone, excel clone, a lot of hybrid mobile applications, UI for a social monitoring system, comment moderation system, ticket booking system, a lot of admin UIs and more. Sometimes we make isomorphic apps too. The fundamental difference between regular SPA and isomorphic SPA is that in isomorphic SPA you will process several requests simultaneously, therefore you should somehow deal with a global user-dependent state (like current language, flux stores state etc).</p>
<p><a href="http://itsquiz.com/en/activations" target="_blank" rel="external">itsquiz.com</a> is one of our projects written in ReactJS. itsquiz.com is a cloud testing platform with a lot of amazing features. And one of the key features of the product is a public quizzes catalogue (aka “quizwall”), any user can publish his own tests there and pass others’. For example, you can go there and <a href="http://itsquiz.com/en/activations/5660a8795b8300236895bef5" target="_blank" rel="external">test your knowledge of ReactJS</a>.</p>
<p><em>You can watch 1 minute promo video to better understand the idea of the product:</em></p>
<p><a href="http://www.youtube.com/watch?feature=player_embedded&v=eiougg2UhYA
" target="_blank"><img src="http://img.youtube.com/vi/eiougg2UhYA/0.jpg" alt="IMAGE ALT TEXT HERE" width="480" height="320" border="10"></a></p>
<p>Here are key requirements to Quizwall:</p>
<ol>
<li>Content is available without authorization.</li>
<li>It should be indexable by search engines.</li>
<li>It should have social networks sharing features.</li>
<li>It should support different languages.</li>
<li>It should work fast</li>
</ol>
<p>Writing isomorphic application is the simplest and the most suitable solution in this case.</p>
<h2 id="What_parts_of_your_app_should_be_isomorphic?">What parts of your app should be isomorphic?</h2><ol>
<li>Isomorphic view</li>
<li>Isomorphic styles</li>
<li>Isomorphic routing</li>
<li>Isomorphic data fetching</li>
<li>Isomorphic configuration</li>
<li>Isomorphic localization</li>
</ol>
<p>Let’s go one after another.</p>
<h3 id="Isomorphic_view_(Joy_#1)">Isomorphic view (Joy #1)</h3><p>This is the simplest part. It is simple because Facebook developers solved this problem already in ReactJS. The only thing we should do is to take React Js library and use it according to documentation.</p>
<p>Client code:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> ReactDOM <span class="keyword">from</span> <span class="string">'react-dom'</span>;</span><br><span class="line"><span class="keyword">import</span> App      <span class="keyword">from</span> <span class="string">'./App'</span>;</span><br><span class="line"></span><br><span class="line">ReactDOM.render(</span><br><span class="line">    <span class="xml"><span class="tag">&lt;<span class="title">App</span> /&gt;</span>,</span><br><span class="line">    document.getElementById('react-view')</span><br><span class="line">);</span></span><br></pre></td></tr></table></figure>
<p>Server code:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> ReactDOM <span class="keyword">from</span> <span class="string">'react-dom/server'</span>;</span><br><span class="line"><span class="keyword">import</span> App      <span class="keyword">from</span> <span class="string">'./App'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> componentHTML = ReactDOM.renderToString(</span><br><span class="line">    <span class="xml"><span class="tag">&lt;<span class="title">App</span> /&gt;</span></span><br><span class="line">)</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> html = <span class="string">`</span><br><span class="line">    &lt;html&gt;</span><br><span class="line">        &lt;head&gt;</span><br><span class="line">            &lt;title&gt;Quiz Wall&lt;/title&gt;</span><br><span class="line">        &lt;/head&gt;</span><br><span class="line">        &lt;body&gt;</span><br><span class="line">            &lt;div id="react-view"&gt;<span class="subst">$&#123;componentHTML&#125;</span>&lt;/div&gt;</span><br><span class="line">        &lt;/body&gt;</span><br><span class="line">    &lt;/html&gt;</span><br><span class="line">`</span>;</span><br><span class="line"></span><br><span class="line">res.end(html)</span><br></pre></td></tr></table></figure>
<p>As you see, we just use “ReactDOM.renderToString” instead of “ReactDOM.render”. That’s it. Nothing complex and you can find this in any tutorial.</p>
<h3 id="Isomorphic_styles">Isomorphic styles</h3><p>Usually tutorials omit this. And this is the first place where you start to feel pain ;).</p>
<h4 id="Pain_#1:_styles_import">Pain #1: styles import</h4><p>We use webpack and usually we import component specific styles in component itself. For example, if we have a component named “Footer.jsx” then we will have less file named “Footer.less” in the same folder. And “Footer.jsx” will import “Footer.less”. The component will have class by its name (“Footer”) and all styles will be namespaced by this class.</p>
<p>Here is a small example:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">'./Footer.less'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="class"><span class="keyword">class</span> <span class="title">Footer</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            <span class="xml"><span class="tag">&lt;<span class="title">div</span> <span class="attribute">className</span>=<span class="value">'Footer'</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="title">small</span>&gt;</span></span><br><span class="line">                    Developed by</span><br><span class="line">                    <span class="tag">&lt;<span class="title">a</span> <span class="attribute">href</span>=<span class="value">'http://webbylab.com'</span> <span class="attribute">target</span>=<span class="value">'_blank'</span>&gt;</span></span><br><span class="line">                        WebbyLab</span><br><span class="line">                    <span class="tag">&lt;/<span class="title">a</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="title">small</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="title">div</span>&gt;</span></span><br><span class="line">        )</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Such approach makes our code more modular. Moreover, if we import a component it will automatically import its dependencies (js libs, styles, other assets). Webpack is responsible for handling all file types. So, we have self-contained components.</p>
<p>This approach works great with webpack. But it will not work in pure nodejs, because you cannot import “less” files. So I’ve started looking for a solution.</p>
<p>The first possible one was <a href="https://nodejs.org/api/globals.html#globals_require_extensions" target="_blank" rel="external">require.extensions</a> but</p>
<ol>
<li>Feature stability: 0</li>
<li>Status: “deprecated”</li>
<li>Does not work with babel-node. I am not sure why, more investigation required.</li>
</ol>
<p>So, I’ve started looking for another solution. The simplest one was using inline styles.</p>
<p><strong>Inline styles</strong> .</p>
<p>I’ve decided to try inline styles because:</p>
<ol>
<li>Inline styles have no problems with server side import. They can be saved in json files.</li>
<li><a href="https://facebook.github.io/react/tips/inline-styles.html" target="_blank" rel="external">React supports inline styles</a></li>
<li>Inline styles solve a lot of CSS issues without hacks - <a href="https://speakerdeck.com/vjeux/react-css-in-js" target="_blank" rel="external">React: CSS in JS by vjeux</a></li>
</ol>
<p>There are several issues I’ve faced using them:</p>
<ol>
<li>You should emulate pseudo css attributes like :hover, :active, :focus with JavaScript.</li>
<li>You should manage vendor prefixes by your own</li>
<li>You should emulate media queries with JavaScript</li>
<li>You need to merge styles some way. (with css you usually just mention several classes names)</li>
</ol>
<p>I’ve found a great tool for working with inline styles called <a href="http://projects.formidablelabs.com/radium/" target="_blank" rel="external">Radium</a>. It is great tool which solves all mentioned issues if you develop SPA.</p>
<h4 id="Pain_#2:_automatic_vendor_prefixing_based_on_browser_DOM">Pain #2: automatic vendor prefixing based on browser DOM</h4><p>We’ve switched to Radium but when we run our application in isomorphic mode we received strange warnings.</p>
<p><img src="markup-error.png" alt=""></p>
<p>“React injected new markup to compensate which works but you have lost many of the benefits of server rendering.” No, I want all the benefits of server rendering. We run the same code on a server and a client, so why react generates different markup? The problem is with Radium automatic vendor prefixing. Radium creates DOM element to detect list of css properties that should have vendor prefixes.</p>
<p>Here is the issue on Github <a href="https://github.com/FormidableLabs/radium/issues/201" target="_blank" rel="external">“Prefixing breaks server rendering”</a>. Yes, there is a solution for it now: using Radium’s autoprefixer on client side and detect browser by user-agent and insert different prefixes (with <a href="https://github.com/rofrischmann/inline-style-prefixer" target="_blank" rel="external">inline-style-prefixer</a>) for requests from different browser on server. I tried, but that time the solution was not reliable. Maybe now it works better (you can check it by your own :)).</p>
<p>The second problem is that you cannot use media queries. Your server does not have any information about your browser window size, resolution, orientation etc. Here is a related issue <a href="https://github.com/FormidableLabs/radium/issues/53" target="_blank" rel="external">https://github.com/FormidableLabs/radium/issues/53</a>.</p>
<p><strong>Solution that works</strong></p>
<p>I’ve decided to switch back to <a href="http://lesscss.org/" target="_blank" rel="external">less</a> and <a href="http://getbem.com/" target="_blank" rel="external">BEM</a> but with condititional import.</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( process.env.BROWSER ) &#123;</span><br><span class="line">    <span class="built_in">require</span>(<span class="string">'./Footer.less'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="class"><span class="keyword">class</span> <span class="title">Footer</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            <span class="xml"><span class="tag">&lt;<span class="title">div</span> <span class="attribute">className</span>=<span class="value">'Footer'</span>&gt;</span> <span class="tag">&lt;/<span class="title">div</span>&gt;</span></span><br><span class="line">        )</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>You see that we are using “require” instead of “import” to make it runtime dependendant, so nodejs will not require it when you run code on server.</p>
<p>One more thing we need to do is to define process.env.BROWSER in our webpack config. It can be done in the following way:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> webpack = <span class="built_in">require</span>(<span class="string">'webpack'</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">    entry: <span class="string">"./client/app.js"</span>,</span><br><span class="line">    plugins: [</span><br><span class="line">        <span class="keyword">new</span> webpack.DefinePlugin(&#123;</span><br><span class="line">            <span class="string">"process.env"</span>: &#123;</span><br><span class="line">                BROWSER: <span class="built_in">JSON</span>.stringify(<span class="literal">true</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;),</span><br><span class="line">        <span class="keyword">new</span> ExtractTextPlugin(<span class="string">"[name].css"</span>)</span><br><span class="line">    ],</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>You can find whole <a href="https://github.com/WebbyLab/itsquiz-wall/blob/master/webpack.config.js" target="_blank" rel="external">production config on github</a>.</p>
<p>Alternative solution is to create a plugin for babel that will just return “{}” on the server. I am not sure that it possible to do. If you can create babel-stub-plugin - it will be awesome.</p>
<p><strong>UPDATE: We’ve switched to the alternative solution after migrating to Babel 6</strong></p>
<p>We use <a href="https://www.npmjs.com/package/babel-plugin-transform-require-ignore" target="_blank" rel="external">babel-plugin-transform-require-ignore</a> plugin for Babel 6. Special thanks to @morlay (Morlay Null) for the plugin.</p>
<p>All you need is to configure file extentions that should be ignored by babel in  <a href="https://github.com/WebbyLab/itsquiz-wall/blob/master/.babelrc" target="_blank" rel="external">.babelrc</a></p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">"env"</span>: &#123;</span><br><span class="line">   <span class="string">"node"</span>: &#123;</span><br><span class="line">     <span class="string">"plugins"</span>: [</span><br><span class="line">       [</span><br><span class="line">         <span class="string">"babel-plugin-transform-require-ignore"</span>, &#123; <span class="string">"extensions"</span>: [<span class="string">".less"</span>, <span class="string">".css"</span>] &#125;</span><br><span class="line">       ]</span><br><span class="line">     ]</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>and set environment variable BABEL_ENV=’node’ before starting your app. So, you can start your app like that <a href="https://github.com/WebbyLab/itsquiz-wall/blob/7fb861ca09a4759b1b3622ab69d0cd610303da8e/package.json#L14" target="_blank" rel="external"><code>cross-env BABEL_ENV=&#39;node&#39; nodemon server/runner.js</code></a>.</p>
<h4 id="Pain_#3:_Material_UI_uses_vendor_prefixing_based_on_browser_DOM">Pain #3: Material UI uses vendor prefixing based on browser DOM</h4><p>Ok. Let’s go further. We managed our styles. And it seems that the problems with styles are solved. We use <a href="http://material-ui.com/" target="_blank" rel="external">Material UI</a> components library for our UI and we like it. But the problem with it is that it uses the same approach to vendor autoprefixing as Radium.</p>
<p>So we had to switch to the Material Design Lite. We use <a href="https://www.npmjs.com/package/react-mdl" target="_blank" rel="external">react-mdl</a> wrapper for React.</p>
<p>Great, it seems that we definitely solved all of the problems related to styling… sorry, not this time.</p>
<h4 id="Pain_#3:_Assets_loading_order">Pain #3: Assets loading order</h4><p>Webpack will generate a javascript bundle for you and will pack all css files to the same bundle. It is not a problem in SPA - you just load the bundle and start your app. With isomorphic SPA everything is not so obvious.</p>
<p>Firstly, it is a good idea to move your bundle.js to the end of markup. In this case, user will not wait untill large (it can be several megabytes) bundle.js is loaded. A browser will render HTML immediately.</p>
<figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="title">html</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="title">head</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="title">title</span>&gt;</span>Quiz Wall<span class="tag">&lt;/<span class="title">title</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="title">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="title">body</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="title">div</span> <span class="attribute">id</span>=<span class="value">"react-view"</span>&gt;</span>$&#123;componentHTML&#125;<span class="tag">&lt;/<span class="title">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="title">script</span> <span class="attribute">type</span>=<span class="value">"application/javascript"</span> <span class="attribute">src</span>=<span class="value">"/build/bundle.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="title">script</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="title">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="title">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>This works. But moving the bundle.js to the end also moves styles to the end (as they are packed into the same bundle). So, a browser will render markup without CSS and after that it will load bundle.js (with CSS in it) and only after that it will apply styles. In this case, you will get blinking UI.</p>
<p>Therefore, the right way is to split your bundle into two parts and load everything in the following order:</p>
<ol>
<li>Load CSS</li>
<li>Load components HTML markup</li>
<li>Load JS</li>
</ol>
<figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="title">html</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="title">head</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="title">title</span>&gt;</span>Quiz Wall<span class="tag">&lt;/<span class="title">title</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="title">link</span> <span class="attribute">rel</span>=<span class="value">"stylesheet"</span> <span class="attribute">href</span>=<span class="value">"/build/bundle.css"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="title">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="title">body</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="title">div</span> <span class="attribute">id</span>=<span class="value">"react-view"</span>&gt;</span>$&#123;componentHTML&#125;<span class="tag">&lt;/<span class="title">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="title">script</span> <span class="attribute">type</span>=<span class="value">"application/javascript"</span> <span class="attribute">src</span>=<span class="value">"/build/bundle.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="title">script</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="title">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="title">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>And the cool thing about webpack is that it has a lot of plugins and loaders. We use <a href="https://www.npmjs.com/package/extract-text-webpack-plugin" target="_blank" rel="external">extract-text-webpack-plugin</a> to extract css to a separate bundle.</p>
<p>Your config will look similar to this one.<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> webpack = <span class="built_in">require</span>(<span class="string">'webpack'</span>);</span><br><span class="line"><span class="keyword">var</span> ExtractTextPlugin = <span class="built_in">require</span>(<span class="string">"extract-text-webpack-plugin"</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">    entry: <span class="string">"./client/app.js"</span>,</span><br><span class="line">    plugins: [</span><br><span class="line">        <span class="keyword">new</span> ExtractTextPlugin(<span class="string">"[name].css"</span>)</span><br><span class="line">    ],</span><br><span class="line">    output: &#123;</span><br><span class="line">        path: __dirname + <span class="string">'/public/build/'</span>,</span><br><span class="line">        filename: <span class="string">"bundle.js"</span>,</span><br><span class="line">        publicPath: <span class="string">"build/"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="built_in">module</span>: &#123;</span><br><span class="line">        loaders: [</span><br><span class="line">            &#123;</span><br><span class="line">                test: <span class="regexp">/\.less$/</span>,</span><br><span class="line">                loader: ExtractTextPlugin.extract(<span class="string">"style-loader"</span>, <span class="string">"css-loader!autoprefixer-loader!less-loader"</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        ]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>You can find whole <a href="https://github.com/WebbyLab/itsquiz-wall/blob/master/webpack.config.js" target="_blank" rel="external">production webpack config on github</a>.</p>
<h3 id="Isomorphic_routing">Isomorphic routing</h3><h4 id="Joy_#2_-_React_Router">Joy #2 - React Router</h4><p>React router starting from version 1.0.0 works greatly in isomorphic environment. But there are a lot of outdated tutorials written when react-router-1.0.0 was still in beta. Don’t worry, official documentation has <a href="https://github.com/rackt/react-router/blob/master/docs/guides/advanced/ServerRendering.md" target="_blank" rel="external">working example of server-side routing</a>.</p>
<h3 id="Isomorphic_data_fetching">Isomorphic data fetching</h3><h4 id="Joy_#3_-_Redux">Joy #3 - Redux</h4><p><a href="https://github.com/rackt/redux" target="_blank" rel="external">Redux</a> is another library that works greatly in isomorphic environment. The main issues with isomorphic apps:</p>
<ol>
<li>Server processes several requests simultaneously. So, you should have isolated state for each request. No singleton flux stores in this case.</li>
<li>You should create new stores for each request.</li>
<li>You should dump all stores states at the end of request processing and pass this states to the browser. So, a browser will be able to fill existing flux stores with received state and rerender React tree.</li>
</ol>
<p>With redux it can be done easily:</p>
<ol>
<li>Just one store</li>
<li>react-redux uses react context to pass request related store way down by react components tree</li>
<li>redux store has convenient API for dumping and restoring store state.</li>
</ol>
<p><a href="https://github.com/WebbyLab/itsquiz-wall/blob/master/server/app.js" target="_blank" rel="external">Here you can find working code</a></p>
<h3 id="Data_fetching">Data fetching</h3><p>You should write code that works both on the server and on the client. Usually in SPAs (even not isomorphic) we write API layer which can be used on the server too. This layer is responsible for all communications with the REST API. It can be packed as separate library for using it in third-party projects.</p>
<p>Here is an example that works both on server and on client:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="pi">'use strict'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> apiFactory <span class="keyword">from</span> <span class="string">'./api'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> api = apiFactory(&#123;</span><br><span class="line">    apiPrefix: <span class="string">'http://itsquiz.com/api/v1'</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Can be use in a following manner</span></span><br><span class="line"><span class="keyword">const</span> promise = api.users.list();</span><br></pre></td></tr></table></figure>
<p>For making http request you can use something like <a href="https://www.npmjs.com/package/axios" target="_blank" rel="external">axios</a> but I prefer <a href="https://www.npmjs.com/package/isomorphic-fetch" target="_blank" rel="external">isomorphic-fetch</a> which uses <a href="">whatwg-fetch</a> (from Github) in browser or <a href="https://www.npmjs.com/package/node-fetch" target="_blank" rel="external">node-fetch</a> on server.<br>“fetch” is a <a href="https://developer.mozilla.org/en/docs/Web/API/Fetch_API" target="_blank" rel="external">standard</a> that is already <a href="http://caniuse.com/#search=fetch" target="_blank" rel="external">supported by firefox and chrome</a> natively.</p>
<p>It is the easy part. More complex part is not to create api library but to use it in isomorphic environment.</p>
<p><strong>How client usually works</strong></p>
<ol>
<li>React component rendering.</li>
<li>Show loading spinner</li>
<li>Fetch all the component (pgge) dependent data</li>
<li>Update the page (rerender React component with fetched data)</li>
</ol>
<p>So, the idea is simple. A user will wait for data but will not wait for UI response. So, you should render immediately without data and show spinner and show the data when it was fetched.</p>
<p><strong>How server usually works</strong></p>
<ol>
<li>Preload all required data for the page</li>
<li>Render the page (with data) to string</li>
<li>Send HTML markup to client</li>
</ol>
<p>We want to write the same code for two scenarios. How do we handle this?<br>The idea is simple. We use action creators and they trigger data fetching. So, we should describe all page dependencies - action creators that will be used for data fetching.</p>
<p>The isomorphic part of code will look like:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="pi">'use strict'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> React               <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; connect &#125;         <span class="keyword">from</span> <span class="string">'react-redux'</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; loadActivations &#125; <span class="keyword">from</span> <span class="string">'actions/activations'</span>;</span><br><span class="line"><span class="keyword">import</span> connectDataFetchers <span class="keyword">from</span> <span class="string">'lib/connectDataFetchers.jsx'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> ActivationsPage <span class="keyword">from</span> <span class="string">'components/pages/ActivationsPage.jsx'</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ActivationsPageContainer</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            <span class="xml"><span class="tag">&lt;<span class="title">ActivationsPage</span></span><br><span class="line">                <span class="attribute">activations</span> = &#123;<span class="attribute">this.props.activations</span>&#125;</span><br><span class="line">                <span class="attribute">search</span>      = &#123;<span class="attribute">this.props.search</span>&#125;</span><br><span class="line">                <span class="attribute">onItemClick</span> = &#123;<span class="attribute">this.handleQuizCardClick</span>&#125;</span><br><span class="line">                <span class="attribute">onSearch</span>    = &#123;<span class="attribute">this.handleSearch</span>&#125;</span><br><span class="line">            /&gt;</span></span><br><span class="line">        )</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> connect(&#123;activations : state.activations&#125;)(</span><br><span class="line">    connectDataFetchers(ActivationsPageContainer, [ loadActivations ])</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
<p>So, you should wrap your component in another one which will be responsible for data fetching. We use “connectDataFetchers” function for it. It takes React component class and array of references to action creators.</p>
<p><strong>How it works?</strong></p>
<p>In browser the wrapper component will call action creators in <strong>componentDidMount</strong> lifecycle hook.  So, it is a common pattern for SPA.</p>
<p><em>IMPORTANT: componentWillMount is not suitable for this because it will be invoked on the client and server. componentDidMount will be invoked only on the client.</em></p>
<p>On the server we do this in a different way. We have a function <a href="https://github.com/WebbyLab/itsquiz-wall/blob/master/server/utils.js#L12" target="_blank" rel="external">“fetchComponentsData”</a> which takes an array of components you are going to render and calls static method “fetchData” on each. One important thing is a usage of promises. We use promises to postpone rendering until the required data is fetched and saved to the redux store.</p>
<p>“connectDataFetchers” is extremely simple:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> IS_FIRST_MOUNT_AFTER_LOAD = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span> <span class="title">connectDataFetchers</span>(<span class="params">Component, actionCreators</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">DataFetchersWrapper</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">        static fetchData(&#123; dispatch, params = &#123;&#125;, query = &#123;&#125; &#125;) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">Promise</span>.all(</span><br><span class="line">                actionCreators.map(actionCreator =&gt; dispatch(actionCreator(&#123; params, query &#125;)))</span><br><span class="line">            );</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        componentDidMount() &#123;</span><br><span class="line">            <span class="comment">// If the component is mounted first time</span></span><br><span class="line">            <span class="comment">// do no fetch data as it is already fetched on the server.</span></span><br><span class="line">            <span class="keyword">if</span> (!IS_FIRST_MOUNT_AFTER_LOAD) &#123;</span><br><span class="line">                <span class="keyword">this</span>._fetchDataOnClient();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            IS_FIRST_MOUNT_AFTER_LOAD = <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        componentDidUpdate(prevProps) &#123;</span><br><span class="line">            <span class="comment">// Refetch data if url was changed but the component is the same</span></span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> &#123; location &#125; = <span class="keyword">this</span>.props;</span><br><span class="line">            <span class="keyword">const</span> &#123; location: prevLocation &#125; = prevProps;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> isUrlChanged = (location.pathname !== prevLocation.pathname)</span><br><span class="line">                              || (location.search !== prevLocation.search);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (isUrlChanged) &#123;</span><br><span class="line">                <span class="keyword">this</span>._fetchDataOnClient();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        _fetchDataOnClient() &#123;</span><br><span class="line">            DataFetchersWrapper.fetchData(&#123;</span><br><span class="line">                dispatch : <span class="keyword">this</span>.props.dispatch,</span><br><span class="line">                params   : <span class="keyword">this</span>.props.params,</span><br><span class="line">                query    : <span class="keyword">this</span>.props.location.query</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        render() &#123;</span><br><span class="line">            <span class="keyword">return</span> (</span><br><span class="line">                <span class="xml"><span class="tag">&lt;<span class="title">Component</span> &#123;<span class="attribute">...this.props</span>&#125; /&gt;</span></span><br><span class="line">            )</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><a href="https://github.com/WebbyLab/itsquiz-wall/blob/master/shared/lib/connectDataFetchers.jsx" target="_blank" rel="external">Production version</a> a little bit longer. It passes locale information and has propTypes described.</p>
<p>So, on server our code looks like:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// On every request</span></span><br><span class="line"><span class="keyword">const</span> store = configureStore();</span><br><span class="line"></span><br><span class="line">match(&#123; routes, location: req.url &#125;, (error, redirectLocation, &#123;components, params, location&#125;) =&gt; &#123;</span><br><span class="line">    fetchComponentsData(store.dispatch, components, params, location.query).then(() =&gt; &#123;</span><br><span class="line">        <span class="keyword">const</span> componentHTML = ReactDOM.renderToString(</span><br><span class="line">            <span class="xml"><span class="tag">&lt;<span class="title">Provider</span> <span class="attribute">store</span>=<span class="value">&#123;store&#125;</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="title">RoutingContext</span> &#123;<span class="attribute">...renderProps</span>&#125;/&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="title">Provider</span>&gt;</span></span><br><span class="line">        )</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> renderHTML(&#123;</span><br><span class="line">            componentHTML,</span><br><span class="line">            initialState: store.getState()</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>Here is the whole server app - <a href="https://github.com/WebbyLab/itsquiz-wall/blob/master/server/app.js" target="_blank" rel="external">https://github.com/WebbyLab/itsquiz-wall/blob/master/server/app.js</a></p>
<h3 id="Isomorphic_configuration">Isomorphic configuration</h3><p>The simplest way it to use “config.json” and require it wherever needed. And you can find that a lot of people are doing so. In my opinion, this is a bad solution for isomorphic SPA.</p>
<p>What wrong with it?</p>
<p>The problem is that when you require “config.json” webpack will pack it into your bundle.</p>
<ol>
<li>You cannot change config without rebuilding the app. It is not an option for me because I want to use the same build on staging and later on production the only difference is configuration options.</li>
<li>You can have inconsistent config state. Backend sees the new one, but frontend does not see changes because the config is packed into the bundle.</li>
</ol>
<p>The solution is to leave config outside the bundle and place it in some sort of global variable that can be set in index.html.</p>
<p>We load out config on the server and return it in index.html</p>
<figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="title">div</span> <span class="attribute">id</span>=<span class="value">"react-view"</span>&gt;</span>$&#123;componentHTML&#125;<span class="tag">&lt;/<span class="title">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="title">script</span> <span class="attribute">type</span>=<span class="value">"application/javascript"</span>&gt;</span><span class="coffeescript"></span><br><span class="line">    <span class="built_in">window</span>.__CONFIG__ = $&#123;serializeJs(config, &#123; <span class="attribute">isJSON</span>: <span class="literal">true</span> &#125;)&#125;;</span><br><span class="line">    <span class="built_in">window</span>.__INITIAL_STATE__ = $&#123;serializeJs(initialState, &#123; <span class="attribute">isJSON</span>: <span class="literal">true</span> &#125;)&#125;;</span><br><span class="line"></span><span class="tag">&lt;/<span class="title">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="title">script</span> <span class="attribute">type</span>=<span class="value">"application/javascript"</span> <span class="attribute">src</span>=<span class="value">"$&#123;config.staticUrl&#125;/static/build/main.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="title">script</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><em>IMPORTANT:  Serializing initialState with JSON.stringify will make your application vulnerable to XSS attacks!!! You should use <a href="https://github.com/yahoo/serialize-javascript" target="_blank" rel="external">serialize-javascript</a> instead!</em></p>
<p>But depending on a global variable in your code is not a good idea. So, we create “config.js” module that just exports global variable. And our code depends on “config.js” module.  Our “config.js” should be isomorphic, so on the server we just require json file.</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// config.js</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (process.env.BROWSER) &#123;</span><br><span class="line">    <span class="built_in">module</span>.exports = <span class="built_in">window</span>.__CONFIG__;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">module</span>.exports = <span class="built_in">require</span>(<span class="string">'../etc/client-config.json'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>and we use the “config.js” in the following manner in our isomorphic code:</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="pi">'use strict'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> config     <span class="keyword">from</span> <span class="string">'./config'</span>;</span><br><span class="line"><span class="keyword">import</span> apiFactory <span class="keyword">from</span> <span class="string">'./api'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> apiFactory(&#123;</span><br><span class="line">    apiPrefix: config.apiPrefix</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<h3 id="Isomorphic_localization">Isomorphic localization</h3><p>Very few tutorials explain how to deal with localization in a regular SPA. No tutorials at all say how to deal with localization in an isomorphic environment. In general, it is not an issue for most of the developers because there is no need to support other languages except English. But it is really important topic, so I’ve decided to describe localization issues in the separate post. It will be end to end React applications localization guide (including isomorphic issues).</p>
<h2 id="Statistics">Statistics</h2><p>Universal (isomorphic) code - 2396 SLOC (93.3%)<br>Client specific code - 33 SLOC (1.2%)<br>Server specific code - 139 SLOC (5.4%)</p>
<p>While all codebase growths, isomorphic part of the code growths the most. So, code reuse rate will become higher with time.</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>This post is not a tutorial. There are enough of them on the Internet. It is always more interesting to look at real production app. A lo]]>
    </summary>
    
      <category term="Isomorphic" scheme="http://blog.koorchik.com/tags/Isomorphic/"/>
    
      <category term="JavaScript" scheme="http://blog.koorchik.com/tags/JavaScript/"/>
    
      <category term="ReactJS" scheme="http://blog.koorchik.com/tags/ReactJS/"/>
    
      <category term="Redux" scheme="http://blog.koorchik.com/tags/Redux/"/>
    
  </entry>
  
</feed>